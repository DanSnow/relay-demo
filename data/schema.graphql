type Query {
  records: [Record]
  recordList: RecordList
  record(id: String!): Record
}

type Record {
  id: ID
  name: String
  phone: String
  email: String
  country: String
}

type RecordList {
  count: Int
  records: [Record]
}
